# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Weather Info' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Weather Info
	pod 'GoogleMaps'
	pod 'GooglePlaces'
	pod 'SDWebImage', '~>3.8'

  target 'Weather InfoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Weather InfoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
